{
  "name": "project-recorder",
  "version": "1.0.0",
  "description": "Single-page speech-to-text workspace tool (Cloudflare Pages + Functions)",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('No build step: Cloudflare serves ./public and ./functions directly')\"",
    "start": "npm run dev",
    "dev": "wrangler pages dev --kv RECORDER_KV",
    "deploy": "wrangler pages deploy",
    "kv:create": "wrangler kv namespace create RECORDER_KV"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
